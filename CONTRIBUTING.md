# 贡献指南 (Contributing)

感谢关注 `undergrad-physics-skill`。本技能的核心纪律是**验证门禁**——没有经过 F/L/B/C 验证的答案不会被输出。贡献（尤其是例题）必须遵守同一纪律。

## 收录标准（例题必须全部满足）

新例题提交前逐项自查，任何一项不满足即退回：

1. **模板 A 六节完整且顺序正确**：`题意与图景`、`建模`、`推导`、`验算`、`答案`、`易错点`，每节独立成节。
2. **验算节逐条编号**：列出最小充分验算集——通常 F、B 加一项独立检查，本域必做 J 必须加入；每条含可复核的具体检查（表达式/代入值/极限）与 `PASS`/`FAIL`/`N/A（原因）`。
3. **答案节显式加粗**：用 Markdown `**...**` 包裹完整结论（含单位与适用条件）；`\boxed{}` 不替代加粗。
4. **附验算摘要行**：列出实际检查，例如 `验算：①F、②B、③L，FAIL 0 项`（不适用项写 `N/A（原因）`）。
5. **交付格式匹配**：Markdown 模式使用 `$$ ... $$` 块；用户要求编译文档时使用 LaTeX 文档模式。
6. **物理正确性**：最小充分验算集中的每项都实际执行；PASS 附具体依据。

## 例题 PR 模板

PR 标题格式：`[例题] <领域>/<题型>`，例如 `[例题] 电磁学/介质边界`。

PR 正文请包含：

- 变更类型：新例题 / 模块内容改进 / 测试与工具 / 文档
- 题目来源：自拟或教科书通用题（不要整段搬运教材原文）
- 覆盖缺口：对照 `modules/` 说明补了哪个空白
- 验证组合：F、B 与独立检查是否构成最小充分集；本域必做 J 是否包含
- 自查清单：模板 A 六节、验算编号、答案加粗、验算摘要、无禁用符号、物理正确性

仓库提供 [PULL_REQUEST_TEMPLATE.md](.github/PULL_REQUEST_TEMPLATE.md)，提交时自动带入。

## 文本与格式风格指南

- **编码**：UTF-8 无 BOM；换行用 LF。
- **Markdown**：标题层级语义化；表格只放结构化对照；禁止用 emoji 或对勾/叉号装饰。
- **LaTeX**：独立公式用 `$$ ... $$` 块；行内 `$` 仅限短符号；只用标准宏（`\frac`、`\sqrt`、`\mathrm`、`\begin{cases}` 等）；不输出 Unicode 数学符号。
- **中文术语**：与现有模块保持一致，例如 拉格朗日量、广义坐标、简正模式、定态、回代、量纲；验证结果统一用 `PASS`/`FAIL`。
- **验证纪律**：每条 PASS 必须附可复核的具体检查；纯结论式 PASS 视为 FAIL。

## 新增领域模块的标准流程

1. **范围决策**：先在 SKILL.md 适用范围/范围外写清边界，避免越界内容。
2. **建模块**：按领域标准结构（适用范围、方法选择表、子节、常见错误、常用公式与陷阱）创建 `modules/<domain>.md`。
3. **注册索引**：在 SKILL.md 模块索引加入对应行。
4. **补测试**：在 `tests/test_cases.md` 按 TC 命名规范补断言。
5. **补例题**：每类方法至少一道模板 A 完整例题，并更新 README 示例数。
6. **跑门禁**：`validate_structure.py`、`numeric_regression.py`、`size_budget.py` 全部通过；英文模块同步更新。

## TC 命名规范

- 格式：`TC-XXX-NNN`。
- `XXX` 为领域/类别码：`NEW`（牛顿）、`LAG`（拉格朗日）、`HAM`（哈密顿）、`CON`（守恒）、`SMO`（小振动）、`NIF`（非惯性系）、`VER`（验证回溯）、`DOM`（定义域）、`ELE`（电磁）、`QNT`（量子）、`TUT`（学生诊断）。
- `NNN` 为三位序号，从 `001` 开始，同一类别内不重复。
- 每个 TC 至少含：Input、答案或 Expected Behavior、验算要点或 Required Verification、Key Checks、Failure Modes。

## 提交流程

1. Fork 并新建分支。
2. 按上述标准修改或新增文件。
3. 本地跑三个门禁脚本，全部通过后再提交。
4. 提交 PR，使用 `[例题]`/`[模块]`/`[测试]`/`[文档]` 前缀，描述变更与验证结果。

## 题目措辞

例题请使用自拟措辞的教科书通用问题（双摆、无限深势阱、RC 放电等），**不要整段搬运** Griffiths、Landau 等教材的题面原文。

## 许可

脚本（如 `tests/validate_structure.py`）按 [MIT](LICENSE) 授权；模块、例题与测试的文本按 [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/) 授权。详见 [NOTICE](NOTICE)。

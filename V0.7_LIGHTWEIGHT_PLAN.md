# v0.7 轻量改进计划

目标：在不扩大 skill 体积和职责的前提下，把当前版本从“规则完整”提升到“分支清楚、规则集中、行为可抽查”。

## 1. 增加顶层路由

在 `SKILL.md` 的核心流程前增加一个 4–6 行的路由说明：

| 用户意图 | 入口 |
|---|---|
| 完整求解/推导 | Template A + 核心流程 |
| 只要结果 | Template B + 最小必要验算 |
| 概念问答 | Template D + 最小示例 |
| 检查/诊断作答 | Template C/E + 学生诊断模块 |
| 要求 Overleaf 文档 | 在上述入口基础上切换 LaTeX 文档模式 |

验收：每个入口说明是否运行完整核心流程；不能再出现“所有请求默认走同一套六节输出”的歧义。

## 2. 收敛验算规则的唯一来源

以 `modules/verification_engine.md` 作为“最小充分验算集”的唯一完整定义。

- `SKILL.md` 只保留一行指针和选择原则。
- `modules/output_templates.md` 只保留输出格式要求，不重复完整验算政策。
- 领域模块只保留本领域推荐组合，例如 `F + B + J`，不重复总规则。
- 中英文文件保持同一策略：通常 `F + B + 一个独立检查`，领域必做 `J` 不可省略。

验收：修改验算政策时只需编辑验证引擎和必要的领域组合；全文搜索不得出现互相冲突的固定 F/L/B/C 规则。

## 3. 保持小规模行为抽查

保留现有 `tests/adversarial_cases.md` 的 6 个案例，再增加最多 4 个高价值案例，不建立大型评测平台。

建议新增：

1. 简单题走 Template B，不展开六节完整答案；
2. 概念题走 Template D，不强行套用完整推导；
3. 诊断题发现第一处错误，不自动泄漏完整答案；
4. LaTeX 文档模式不混用 Markdown 标记。

验收：人工抽查 10 个案例；每个案例记录“应触发的入口、应执行的验算、不可出现的行为”。结构校验器继续负责格式，人工抽查负责行为。

## 范围控制

- 不拆分新的领域 skill。
- 不建立自动 LLM 评测框架。
- 不增加 `agents/openai.yaml`、数据库或外部依赖。
- 不扩展热学、光学或研究级物理范围。

## 完成条件

- [x] 顶层路由已加入；5 个入口映射已逐项核对（行为抽查见 tests/adversarial_cases.md）
- [x] 验算政策已收敛，中文/英文无冲突规则
- [x] 对抗案例 10 个已建档；行为抽查已执行并记录（发布前建议真人复跑一次）
- [x] `SKILL.md`、英文副本和所有模块仍满足现有体积预算
